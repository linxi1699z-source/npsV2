请使用 /Users/xiemin/Documents/codex/原型助手/skills/axure-pay-admin-prototype 这个 skill，
按支付后台 Axure 风格，为【NPS管理】设计后台原型。

页面目标：
- 这个页面用于app 端或者web端配置问卷计划和任务，主要分为计划和任务两部分。一个计划可以包含多个任务。

导航：
- 一级导航：NPS管理
- 二级导航：NPS任务，二级页面中有两个tab页，一个tab是 NPS 任务，一个 tab 是 NPS 计划。点击tab 进去对应动态面板，动态面板显示不同数据。

先按以下prompt输出 NPS 任务页面原型。

页面需要包含：
- 搜索条件：
	任务名：输入框。
	任务场景：单选下拉框，选项有：全局，睡眠，计划，OSA，生理，血压，AI Partner。默认选项为所有。
	任务时间范围：时间范围选择框，如可选择 2026-06-01 - 2026-06-30。
	任务状态：下拉框，选项有：初始状态，投放中，投放完成，投放异常，暂停。默认选项为所有。
- 顶部按钮：搜索，新增。
- 表格字段：
	任务 ID，任务名称，任务状态，任务场景，投放人群，应用端，投放版本，任务开始时间，任务结束时间，问卷模板，计划ID，计划名称，总用户数，实际投放用户数，曝光用户数，操作列。
	点击【投放人群】，进入投放人群页面，待会再补充细节，先做可点击的 UI 展示。
	点击【问卷模板】，进入对应问卷模板详情页，待会补充细节，先做可点击的 UI 展示。
- 操作列：查看，编辑，暂停。

表单 / 弹窗：
- 暂停弹窗：状态为初始状态，投放中允许暂停。暂停弹窗文案：确认暂停任务吗？暂停后将不会给用户投放问卷。点击确定按钮则将任务状态设置为暂停，并隐藏弹窗。点击取消则直接隐藏弹窗。

下拉框和输入框：
- 单选下拉：任务场景
- 输入框需要包含默认、hover、focus、禁用、错误状态。

中继器：
- 表格数据使用中继器实现。
- 中继器字段：同表格字段，我只提供一个示意，剩下需要你自动生成各个状态或场景对应任务。如
	任务 ID：10001，自增，
	任务名称：全局NPS6月任务
	任务状态：初始状态
	任务场景：睡眠
	投放人群：显示查看，允许点击
	应用端：APP
	投放版本：Android > V4.2, iOS > V4.2
	任务开始时间：2026-06-28
	任务结束时间：2026-07-20
	问卷模板：睡眠NPS模板
	计划 ID：10001
	计划名称：睡眠 NPS 计划
	总用户数：100,000
	实际投放用户数：0
	曝光用户数：0
- 需要支持：【新增 / 编辑 / 删除 / 筛选 / 清空条件 / 状态更新】。
- 操作后需要同步更新表格和关联输入框/下拉框。

跳转交互：
- 点击【新增】跳转到【新增任务页面】，待会补充细节。
- 点击【查看】跳转到【查看任务页面】，待会补充细节。
- 点击【编辑】跳转到【编辑任务页面】，待会补充细节

请输出：
- 导航层级建议
- 页面布局说明
- 元件清单和尺寸
- 搜索区设计
- 表格设计
- 表单和动态面板状态
- 中继器字段、示例数据和交互动作
- Axure 可实现的 OnClick / OnSelectionChange / OnItemLoad 动作清单
```